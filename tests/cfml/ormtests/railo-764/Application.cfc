<cfcomponent extends="ormtests.ORMApplication">

<!--- 	<cfset wardir = expandPath("/") />
	<cfset mxunitdir = expandPath("/mxunit") />
	<cfset testdir = expandPath("/tests") />

	<cfset this.mappings['/railo-context'] = wardir & "/WEB-INF/railo/context">
	<cfset this.mappings['/WEB-INF'] = wardir &"/WEB-INF">
	<cfset this.mappings['/mxunit'] = mxunitdir />
	<cfset this.mappings['/tests'] = testdir />
	<cfset this.mappings['/'] = "/tests/cfml/ormtests/#dirname#/entity"> --->

</cfcomponent>
